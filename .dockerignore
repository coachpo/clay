# Production image context: keep only runtime source + packaging metadata.
**

# Required by Dockerfile and packaging.
!pyproject.toml
!app/
!app/**
!.dockerignore

# Exclude local Python build artifacts under allowed paths.
app/**/__pycache__/
app/**/*.py[cod]
app/**/*.pyo
app/**/*.pyd
